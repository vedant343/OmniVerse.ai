import { Button } from "@/components/ui/button";
const page = () => {
  return (
    <div>
      <Button>
        <a href="/sign-in">SignIn</a>
      </Button>
    </div>
  );
};

export default page;
